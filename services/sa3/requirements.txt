# Stable Audio 3 localhost backend requirements
#
# Install order:
#   1. install AMD ROCm 7.2.1 SDK wheel set
#   2. install torch/torchaudio 2.9.1+rocm7.2.1 wheels for Python 3.12
#   3. pip install -e . --no-deps          (vendored stable_audio_3 package)
#   4. pip install -r requirements.txt     (this file)
#   5. force-reinstall ROCm torch/torchaudio if a transitive dependency tries
#      to alter it.

# API
flask
flask-cors
requests

# Upstream stable-audio-3 runtime deps
einops>=0.8.2
einops-exts>=0.0.4
numpy>=2.2.6
packaging>=26.0
safetensors>=0.7.0
tqdm>=4.67.3
huggingface-hub>=1.7.1
transformers>=5.8.0
soundfile>=0.13.1

# LoRA training
accelerate>=0.30
dill>=0.3.8
audio-metadata>=0.11
