# Stable Audio (Jerry) localhost backend requirements
#
# Install order for the ROCm preview:
#   1. install AMD ROCm 7.2.1 SDK wheels
#   2. install matching torch/torchaudio/torchvision wheels for Python 3.12
#   3. pip install -e . --no-deps
#   4. pip install -r requirements.txt
#   5. force-reinstall the ROCm Torch wheel set after transitive dependencies
#
# The CUDA lockfile is retained for reference; the ROCm preview uses this file.

# clip uses `from pkg_resources import packaging` — uv venvs lack setuptools,
# so we install packaging directly and shim pkg_resources in api.py
packaging

# Core web framework
flask
flask-cors

# Model loading and inference
diffusers
accelerate
huggingface_hub
# Xet-backed downloads. Every model repo we pull is on Xet storage, and
# without this huggingface_hub logs a warning and falls back to plain HTTP.
hf_xet
safetensors
# 5.11+ imports PyTorch DTensor sharding while constructing T5 models.
# AMD's Windows ROCm 7.2.1 wheel omits torch._C._distributed_c10d.
transformers==5.10.2
k-diffusion

# Audio
librosa
soundfile

# ML deps
einops
einops_exts
alias-free-torch
numpy==1.26.4
scipy
tqdm
pydantic

# stable-audio-tools declared deps
prefigure
pytorch_lightning
PyWavelets
sentencepiece
torchmetrics
v-diffusion-pytorch
vector-quantize-pytorch
webdataset
wandb
