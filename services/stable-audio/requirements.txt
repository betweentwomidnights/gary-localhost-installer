# Stable Audio (Jerry) localhost backend requirements
#
# Install order:
#   1. pip install torch==2.6.0 torchaudio==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
#   2. pip install -e . --no-deps          (stable-audio-tools editable)
#   3. pip install -r requirements.txt     (this file)
#   4. pip install --force-reinstall --no-deps torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#      ^ step 4 is critical: some deps pull CPU torch as transitive dep, this restores CUDA
#
# Lockfile: requirements.lock.txt contains exact frozen versions from a known-good build.

# Core web framework
flask
flask-cors

# Model loading and inference
diffusers
accelerate
huggingface_hub
safetensors
transformers
k-diffusion

# Audio
librosa
soundfile

# ML deps
einops
einops_exts
alias-free-torch
numpy==1.26.4
scipy
tqdm
pydantic

# stable-audio-tools declared deps
prefigure
pytorch_lightning
PyWavelets
sentencepiece
torchmetrics
v-diffusion-pytorch
vector-quantize-pytorch
webdataset
wandb
