# Foundation-1 localhost backend requirements
#
# Install order:
#   1. pip install torch==2.6.0 torchaudio==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
#   2. pip install -e ./RC-stable-audio-tools --no-deps  (RC fork of stable-audio-tools)
#   3. pip install -r requirements.txt     (this file)
#   4. pip install --force-reinstall --no-deps torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#      ^ step 4: restore CUDA torch if transitive deps pulled CPU version
#
# Lockfile: requirements.lock.txt contains exact frozen versions from a known-good build.

# Core web framework
flask
flask-cors

# Audio processing
librosa
soundfile

# Model loading and inference
huggingface_hub
safetensors
transformers
diffusers
accelerate
k-diffusion
einops
einops_exts
scipy
numpy==1.26.4
tqdm

# RC-SAT declared deps
alias-free-torch
descript-audio-codec
vector-quantize-pytorch<=1.9.14
webdataset<=0.2.48
x-transformers<1.27.0
