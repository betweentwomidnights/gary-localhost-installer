# Foundation-1 localhost backend requirements
#
# Install order for the ROCm preview:
#   1. install AMD ROCm 7.2.1 SDK wheels
#   2. install matching torch/torchaudio/torchvision wheels for Python 3.12
#   3. pip install -e ./RC-stable-audio-tools --no-deps
#   4. pip install -r requirements.txt
#   5. force-reinstall the ROCm Torch wheel set after transitive dependencies
#
# The CUDA lockfile is retained for reference; the ROCm preview uses this file.

# clip uses `from pkg_resources import packaging` — uv venvs lack setuptools,
# so we install packaging directly and shim pkg_resources in api_foundation.py
packaging

# Core web framework
flask
flask-cors

# Audio processing
librosa
soundfile

# Model loading and inference
huggingface_hub
safetensors
# 5.11+ imports PyTorch DTensor sharding while constructing T5 models.
# AMD's Windows ROCm 7.2.1 wheel omits torch._C._distributed_c10d.
transformers==5.10.2
diffusers
accelerate
k-diffusion
einops
einops_exts
scipy
numpy==1.26.4
tqdm

# RC-SAT declared deps
alias-free-torch
descript-audio-codec
vector-quantize-pytorch<=1.9.14
webdataset<=0.2.48
x-transformers<1.27.0
