# MelodyFlow (Terry) localhost backend requirements
#
# Install order for the ROCm preview:
#   1. install AMD ROCm 7.2.1 SDK wheels
#   2. install matching torch/torchaudio/torchvision wheels for Python 3.12
#   3. pip install -r requirements.txt
#   4. force-reinstall the ROCm Torch wheel set after transitive dependencies
#
# Note: audiocraft is NOT installed as a package. It lives in ./audiocraft/
# and is imported via sys.path / PYTHONPATH from the working directory.
#
# Lockfile: requirements.lock.txt contains exact frozen versions from a known-good build.

# Core web framework
flask
flask-cors
psutil

# Audio/ML runtime deps
numpy==1.26.4
av
einops
flashy>=0.0.1
hydra-core>=1.1
hydra_colorlog
julius
num2words
sentencepiece
spacy>=3.6.1
huggingface_hub
tqdm
# Newer Transformers versions import Dynamo/FSDP while loading T5. AMD's
# Windows ROCm wheel intentionally omits the distributed backend they require.
transformers==4.39.3
tokenizers==0.15.2
librosa
soundfile
torchmetrics
encodec
protobuf
pyloudnorm
