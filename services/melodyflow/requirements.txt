# MelodyFlow (Terry) localhost backend requirements
#
# Install order:
#   1. pip install torch==2.7.1 torchaudio==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#   2. pip install -r requirements.txt     (this file)
#   3. pip install flash-attn wheel        (see services manifest for exact URL)
#   4. Install xformers SDPA shim          (see setup script)
#   5. pip install --force-reinstall --no-deps torch==2.7.1 torchaudio==2.7.1 torchvision==0.22.1 --index-url https://download.pytorch.org/whl/cu128
#      ^ step 5: restore CUDA torch if transitive deps pulled CPU version
#
# Note: audiocraft is NOT installed as a package. It lives in ./audiocraft/
# and is imported via sys.path / PYTHONPATH from the working directory.
#
# Lockfile: requirements.lock.txt contains exact frozen versions from a known-good build.

# Core web framework
flask
flask-cors
psutil

# Audio/ML runtime deps
numpy==1.26.4
av
einops
flashy>=0.0.1
hydra-core>=1.1
hydra_colorlog
julius
num2words
sentencepiece
spacy>=3.6.1
huggingface_hub
tqdm
transformers>=4.31.0,<5.0.0
librosa
soundfile
torchmetrics
encodec
protobuf
pyloudnorm
