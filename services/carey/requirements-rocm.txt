# Carey (ACE-Step 1.5) dependencies for the Windows ROCm preview.
# PyTorch and the ROCm SDK are installed separately by the service manifest.

# Core ML
transformers>=4.51.0,<4.58.0
diffusers
accelerate>=1.12.0
einops>=0.8.1
safetensors==0.7.0
peft==0.18.1

# Inference
scipy>=1.10.1
soundfile>=0.13.1
loguru>=0.7.3
numba>=0.63.1
vector-quantize-pytorch>=1.27.15
toml
diskcache

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
gradio==6.2.0

# Training imports. Trainer support will be validated separately.
lycoris-lora
lightning>=2.0.0
tensorboard>=2.0.0
matplotlib>=3.7.5

# Model downloading
modelscope
typer-slim>=0.21.1

# CUDA FlashAttention, Triton, torchao, and torchcodec are intentionally absent.
# Carey does not initialize its language model, and inference uses PyTorch SDPA.
